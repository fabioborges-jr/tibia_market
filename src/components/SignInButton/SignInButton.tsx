import { FcGoogle } from "react-icons/fc"

export default function SignInButton() {
    return (
        <button>
            <FcGoogle />
            <p>Sign In with Google</p>
        </button>
    )
}